// this will only work on client component not on the server component

import "client-only"


export const multiply = (a : number , b : number) => a *b;
export const divide = (x : number , y : number) => x / y;